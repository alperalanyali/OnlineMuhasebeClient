import { ValidInputDirective } from './valid-input.directive';

describe('ValidInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidInputDirective();
    expect(directive).toBeTruthy();
  });
});
